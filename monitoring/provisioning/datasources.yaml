apiVersion: 1
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://lair-monitoring-kube-prome-prometheus.monitoring.svc:9090
    isDefault: true
    editable: false

  - name: Loki
    type: loki
    access: proxy
    url: http://lair-loki-gateway.monitoring.svc:80
    editable: false
